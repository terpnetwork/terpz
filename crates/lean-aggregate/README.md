# lean-aggregate

Optional Jiang same-statement FRI compose (N period/vote proofs → one M31 FRI verify).
Copied from the laptop `feat/lean-consensus` worktree (`1b7db1b`) so it is not lost.

This crate is **not wired** into `terpz` / `x/leanval` / `lean-cw-ffi`. Live LEAN-5 fold is Go `x/leanval` plus `lean-stwo-dummy`. Keep this as a research sidecar.
